name: trabajo_flutter
description: Una aplicación Flutter de demostración para Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Puedes agregar assets aquí si necesitas imágenes, iconos, etc.
  # assets:
  #   - images/
  #   - icons/  